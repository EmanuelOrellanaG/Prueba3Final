server:
  port: 8087

spring:
  application:
    name: clase-service

  datasource:
    url: jdbc:mysql://mysql:3306/gym_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: ""
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

jwt:
  secret: mi_clave_super_segura_de_gym_2026_minimo_32_chars


eureka:
  client:
    enabled: false

